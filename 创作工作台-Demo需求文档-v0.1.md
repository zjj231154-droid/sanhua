# AI 创作工作台｜首版 Demo 需求文档

版本：v0.1  
整理日期：2026-09-10  
可行性评审补充：2026-09-10  
用途：作为首版可交互 Demo 的产品、设计和开发依据。  
状态：依据当前对话形成的工作基线；标注为“建议”或“待确认”的内容不是已承诺效果。

## 1. 产品背景与目标

客户经营一家日咖夜酒和一家固定茶馆，使用团队为老板、设计师、编导三人。产品是线上部署、账号登录后使用的客户专用 AI 创作工作台，不开放公众注册。

首版通过三个独立功能板块，让用户在作品区域与专属智能体协作：

1. 产品精修：处理实拍咖啡、调制酒饮等产品照片，支持单张与批量。
2. 品牌创作：利用已有图片和参考素材，制作海报物料与文创衍生设计方案。
3. 短剧脚本：获取对标数据、分析内容，结合客户茶馆真实场景，交付原创且可拍摄的脚本。

核心体验目标：操作连贯、布局稳定、长期使用不易视觉疲劳；助手能理解当前选中的图片、画布或脚本段落，产出可以直接查看和修改。

## 2. 用户、场景与已知工作量

| 用户 | 主要工作 | 工作台职责 |
|---|---|---|
| 老板／客户管理员 | 审看成果、把握方向 | 查看全部项目与待确认作品，审核，管理成员与费用 |
| 设计师 | 饮品照片精修、品牌物料设计 | 使用产品精修、品牌创作及相关素材 |
| 编导 | 研究对标内容、写脚本 | 使用短剧脚本与茶馆场景资料 |

已知工作量：每周约修图 50 张、制作 3 套物料；脚本暂按对话中的每天 3 个作为设计参考。“一个脚本”是独立短视频、单集还是整部剧，尚待确认；每日研究样本数量未明确。

原片可能来自手机或相机，格式、分辨率、单张大小待客户提供。不要以每周 50 张推断单批一定为 50 张。

## 3. Demo 范围与交付方式

### 3.1 首版 Demo 必须呈现

- 登录入口、三人角色与个人工作记录。
- 首页、三个业务板块、公共素材库、茶馆场景资产、管理设置。
- 每个业务板块内嵌专属助手，支持任务卡、上下文选择和结果回写。
- 产品精修的单张、小样确认、批量任务、异常重做与前后对比。
- 品牌创作的需求输入、多方案选择、文字编辑、换图和尺寸适配预览。
- 短剧脚本的对标导入、分析、场景约束、生成、局部修改和交付。
- 自动保存反馈、版本切换、加载／失败／空状态、任务返回后恢复。
- 模型连接、成员、存储用量与操作记录的管理页面。

### 3.2 Demo 实现约定

首版以可交互流程为验收目标，不默认已具备全部生产服务。无 API 密钥或真实素材时，可以使用示例数据和预制结果，必须在对应页面明确显示“演示数据／演示结果”。

| 能力 | Demo 要求 | 正式线上版本要求 |
|---|---|---|
| 登录 | 能验证演示账号并切换权限表现；若仅前端模拟，明确标注 | 服务端鉴权、独立账号、安全密码存储、会话管理 |
| 智能体 | 能展示上下文、处理方案、修改和结果状态；模拟响应需标注 | 真实模型调用、工具执行、状态持久化 |
| 修图与设计 | 预制样例可用于验证操作，原图和结果可比较 | 接真实编辑模型与图像处理工具后用客户样片验证 |
| 对标数据 | 用带来源说明的示例记录或用户导入验证流程 | 根据后续选定渠道接入，不承诺四平台全量采集 |
| 文件与导出 | 可上传查看样例、下载实际可生成的文件 | 私有对象存储、授权下载、持久保存与恢复 |
| 管理设置 | 可编辑配置结构、展示连接和费用状态 | 服务端加密保存密钥，真实连通测试与计量 |

如果 Demo 已部署到公网且保存客户数据，登录、鉴权与文件访问控制必须真实生效，不能以演示登录替代。

### 3.3 本版不包含，保留扩展点

- VI 手册自动解析和完整品牌规则治理：后续加入；本版可手动指定素材、颜色及风格参考。
- 完整短剧视频生成、配音、口型、剪辑、视频时间线：不在本版范围。
- 茶馆 3D 重建、自训模型、多茶馆门店管理。
- 面向公众的多租户 SaaS、注册付费和复杂组织审批。
- 印刷生产级分色、可生产文创结构设计，以及承诺 PSD／AI 原生源文件交付。

## 4. 信息架构与 UI

### 4.1 导航结构

左侧导航：工作台、产品精修、品牌创作、短剧脚本、素材库；底部为管理设置和个人账号。

左上角提供“全部／日咖夜酒／茶馆”内容筛选。它是同一客户下的内容归属，不是两个独立租户；茶馆脚本默认绑定唯一茶馆。切换归属不得丢失未保存编辑，跨归属素材引用需显式选择。

首页结构：简短欢迎区 → 三个等权功能入口 → 继续上次创作／最近作品 → 进行中与待确认事项。无需大面积装饰横幅或复杂经营数据看板。

### 4.2 视觉方向

采用清爽年轻的文创工作室风格，融合日咖夜酒的生活方式摄影与茶馆的材质、留白。以下为暂定设计色值，不代表客户正式 VI：

| 用途 | 暂定方向 |
|---|---|
| 页面背景 | 暖灰白，如 #F5F3EE |
| 内容表面 | 接近白色，如 #FFFEFB |
| 主文字 | 深灰，如 #242824 |
| 主操作 | 墨绿，如 #254B40 |
| 点缀 | 少量橘红，如 #C65A3A |
| 修图画布 | 中性灰，支持明暗切换 |

正文和控件使用清晰黑体；编辑正文建议 16px，辅助信息不低于 12px，主操作信息优先 14px 以上。装饰字体只用于少量首页标题。纸感纹理仅用于装饰区域，不覆盖图片、脚本或控件。颜色和字号最终以可读性测试为准。

### 4.3 编辑布局

固定左导航＋中央作品区＋可折叠右侧助手。建议桌面导航宽约 200px、助手约 340–380px；宽度为原型起点而非硬性限制。窄屏时导航折叠，助手转抽屉；首版以电脑工作为主，手机侧先保证查看和审核可用。

每个板块使用同一骨架，主工作区按任务变化：

- 精修：缩略图网格／大图对比，批量选择后就地出现工具条。
- 创作：画布、方案缩略图、素材选择；文字在画布或属性栏直接编辑。
- 脚本：场次目录、文档、场景参考抽屉；限制阅读行宽。

### 4.4 流畅性与动效

- 当前步骤只有一个视觉突出的主操作，例如“生成小样”或“处理剩余图片”。
- 用预留尺寸的加载占位避免布局跳动；任务完成不自动重排网格。
- 助手回复不强制滚动作图区；保留选中项、滚动位置和输入内容。
- 悬停和点击反馈轻量；不使用持续浮动、闪烁、粒子背景或自动轮播。
- 动效尊重系统减少动态效果设置；状态同时使用图标和文字。
- 提供键盘焦点、明确选中状态、可关闭提示及足够大的点击区域。
- 编辑自动保存并显示状态；删除进入回收站，修改支持撤销或版本恢复。

## 5. 板块一：产品精修

### 5.1 目标

提高设计师对咖啡、调制酒饮实拍照片的处理效率，优先保持杯型、瓶型、饮品及标签真实性。酒类不默认全部是包装酒瓶，按实际照片识别对象。

### 5.2 工作流

上传原图 → 分组并选图 → 输入要求／选择参考样片 → 诊断与精修方案 → 生成代表性小样 → 设计师确认 → 批量执行 → 检查可疑项 → 单张调整 → 导出。

小样建议默认最多 5 张，优先覆盖不同光线、构图和容器；不足 5 张按实际数量。精修配方可保存并复用，单张可覆盖批量规则。

### 5.3 功能

- 基础调整：曝光、白平衡、色调、裁切、水平与透视、适度降噪锐化。
- 局部处理：桌面污点、杂物、背景清理和可控反光处理。
- 商品保护：自动建议保护区，设计师可调整；标签、杯瓶轮廓、拉花等按任务锁定。
- 参考样片：参考色彩、亮度、留白；不同拍摄条件需独立分析，不能直接照搬参数。
- 对比与版本：原图／结果滑动对比、放大检查、按步骤或版本回退。
- 批量结果：待处理、处理中、待确认、需检查、失败、完成；失败仅重做相关图片。

蒸汽、冷凝水、配料等添加属于创意修改，默认关闭，需用户明确选择。OCR 和视觉检查只能发现部分问题，不能承诺自动保证标签和商品百分之百不变。

### 5.4 智能体与工具

精修助手读取选中图片、参考图、保护区、输出规格和历史要求，形成结构化计划，再调用图像处理工具。传统调整与生成式局部编辑按任务分配；队列、重试、文件转换由程序负责。

助手面板显示当前选择数量、修改项、保护项、小样入口、预计消耗和异常说明。费用无法估算时显示未知，不伪造时间或价格。

## 6. 板块二：品牌创作

### 6.1 工作流

选择物料用途与尺寸 → 输入活动信息和必须出现的文字 → 选择产品图／参考素材 → 设计助手提出建议方向 → 选择方向并生成 → 编辑文字与图片 → 多规格适配预览 → 审核与导出。

方向数量建议默认 3 个，可按调用成本调整。

### 6.2 功能边界

- 支持海报、社媒素材、桌卡等物料的代表性模板。
- 文创支持杯套、贴纸、手提袋等概念效果展示，明确区别于可生产文件。
- 产品精修结果可直接引用到品牌创作，不必重复上传。
- 文字、Logo 尽量作为独立元素排版，避免全部烘焙进生成图片。
- 允许修改尺寸、文字、颜色、图片与布局；换尺寸后必须预览，不能承诺所有比例一键无损适配。
- Demo 导出以 PNG／JPG 及可实现的设计记录为主；印刷 PDF 与原生设计源文件能力另行验证。
- VI 尚未导入时，不显示“已通过 VI 合规检查”；后续预留品牌规则读取与检查节点。

设计助手掌握当前画布、选中元素、活动需求和引用素材。建议先预览再应用到正式版本。

## 7. 板块三：短剧脚本

### 7.1 首版交付目标

以可编辑、可拍摄脚本为终点，附必要的拍摄执行信息。全部剧情表演发生在客户现有茶馆真实场景中。

### 7.2 工作流

对标数据导入／渠道获取 → 展示样本与来源 → 分析题材、开头吸引点、人物关系、冲突和转折 → 编导选方向 → 读取茶馆资产与拍摄约束 → 原创大纲 → 脚本 → 场景与内容检查 → 编导局部修改 → 审核／交付。

数据层预留抖音、快手、红果、微信来源字段和连接器接口；本版使用示例或导入数据，真实渠道和采集范围后续确定。没有足够证据时标记“候选对标”，不宣称为全网爆款。平台热度、点赞等不同口径不得直接混为播放量。

### 7.3 茶馆场景资产

只建立一个真实茶馆。区域数量由实地采集决定，不硬性假设存在包间、后厨等空间。每个区域保存名称、照片、空间关系、可容纳演员、可用道具、机位备注、光线噪声、可拍时间及限制。

Demo 可使用明确标注的虚构示例区域；真实交付前替换并确认。脚本每场绑定具体区域，不存在的道具或动作标为待解决。演员人数、出镜角色、单条时长暂作为生成前可填写的约束。

### 7.4 交付与编辑

- 脚本包含标题、概述、人物、目标时长、分场位置、动作、台词、转折和收尾。
- 执行表包含场次、区域、人物、道具、预计时长和场景参考。
- 选中段落或场次可要求重写；展示修改差异、接受、放弃、撤销。
- 对标分析、茶馆信息和剧本文档在同页切换或侧边展开。
- 保留来源记录；相似度提示不作为原创或版权结论的保证。
- Demo 至少支持复制正文和下载文本／Markdown，排版文档导出后续按实际实现增加。

编导助手仅调用本板块配置的数据连接器、资料检索与脚本工具，不因分析内容触发发布或视频制作。

## 8. 智能体与模型接入架构

三个用户可见智能体：精修助手、设计助手、编导助手。共用鉴权、素材、任务、版本和模型接入服务；各自维护任务上下文，不混用不相关对话。

统一流程状态：草稿、排队、执行中、等待用户、已完成、失败、已取消。作品审核状态独立保存，避免“生成完成”等同于“老板已通过”。

每个任务保存任务类型、所属空间、用户、输入素材引用、工作流版本、提示词版本、模型标识、步骤状态、结果版本和成本记录。大文件不塞入对话历史或状态数据库正文。

### 8.1 候选技术方案，尚未最终锁定

| 部分 | 候选 | 选择理由 |
|---|---|---|
| 自定义界面 | React／Next.js | 适合三种工作区及统一布局 |
| 助手与 UI 连接 | CopilotKit | 共享选中对象、任务状态与交互卡片 |
| 智能体流程 | LangGraph | 支持持久状态、人工介入及流程恢复 |
| 模型层 | 先用服务端适配器，按需要加入 LiteLLM | 按能力接入厂商，减少初版组件数量 |
| 可视化验证备选 | Dify | 可用于验证流程，需核对实际许可证和部署条件 |

上述项目的能力与许可证以选定发布版本为准。本项目不要求同时部署多个智能体编排平台。

### 8.2 模型配置

按文本创作、视觉诊断、图片生成、图片编辑分别配置默认服务。具体厂商、模型和预算尚未选择，不承诺模型效果。未来视频能力仅预留类型，不显示可用视频功能。

管理字段：厂商、允许的服务地址、密钥、模型名称、能力、默认用途、超时、并发、费用限制、启用状态及连接测试。图片参考数量、蒙版和异步返回方式由专属适配器处理。

模型密钥保存在服务端；对外只返回脱敏值。备用模型切换须满足相同任务能力、成本和数据处理要求，不得静默使用不适配的模型。批量重试采用任务标识和结果检查，尽量避免重复提交计费。

## 9. 账号、部署、数据与版本维护

### 9.1 账号

默认管理员创建账号，关闭公开注册。密码不可逆哈希存储；登录失败限制、会话过期、密码重置、退出及撤销会话。页面、业务 API、文件访问均校验权限。

当前按公网 HTTPS 访问＋账号限制规划，必要时可叠加 VPN／访问网关。三人使用的具体跨板块权限可由管理员配置。

### 9.2 建议部署结构

网站与业务服务、独立后台任务进程、PostgreSQL、任务队列、私有对象存储。数据库存账号、任务、脚本和文件索引；对象存储存原图、成品、参考资料与场景照片。

采用外部模型 API 时，应用服务不预设 GPU。服务器地区、供应商、规格及预算待确定后再选。

### 9.3 大文件与容量

支持分片／断点续传、缩略图和预览图、授权直传和短期下载链接；上传状态与生成状态分别显示。关闭页面后已提交的后台任务继续执行。

容量示例：50 张／周 ×（30 MB 原图＋3 × 15 MB 成品）约 3.75 GB／周、195 GB／年；该数字只是估算，不包含其他素材、备份及存储版本开销。

原图和正式成果默认保留；临时文件、失败中间件、回收站期限待设定并向管理员说明。存储临界时提醒并提供整理入口，不擅自删除正式作品。

数据库和文件分别备份，避免把对象版本功能等同于独立备份。正式上线前确认恢复目标并验证恢复。用量展示区分存储、下载流量和模型费用。

### 9.4 客户持续升级

业务接口预留版本路径，例如 /api/v1。应用发布、智能体流程、模型配置、数据库迁移各有版本记录。测试与生产分离，迁移前备份并设计兼容和回滚方案。

升级不覆盖客户素材；进行中任务绑定创建时版本。运维使用独立身份和操作审计，不留隐藏维护账号。界面提供更新说明，敏感运维权限与普通管理员权限分开。

## 10. Demo 页面清单与验收

| 页面／状态 | 验收操作 |
|---|---|
| 登录 | 正确账号可进入，错误信息清楚，退出后受保护内容不可继续访问 |
| 工作台 | 三个入口可进入，空间筛选生效，能恢复最近任务 |
| 精修列表 | 上传／选择多图，输入要求，保存配方，显示当前选中范围 |
| 精修执行 | 先小样后批量，已完成项可查看，失败项可单独重试 |
| 精修详情 | 原图结果比较，局部修改，切换版本并下载 |
| 品牌创作 | 填任务、选素材、选方向、编辑文案换图、预览尺寸与导出 |
| 脚本分析 | 导入示例对标，查看来源与分析，选定方向 |
| 脚本编辑 | 绑定茶馆区域，生成正文，修改指定场次，比较并接受修改 |
| 素材库 | 按归属／类型筛选，查看详情，引用到任务，删除进入回收站 |
| 管理设置 | 成员与角色可查看；模型、存储、记录页面状态完整且模拟信息有标注 |
| 共通体验 | 空、加载、失败、保存中、完成状态齐全；无明显布局跳动和操作丢失 |

Demo 验收重点是端到端操作和状态可信。正式效果另用真实样片、历史物料及脚本做评测，关注人工返工时间、一次通过情况、实际脚本采用情况和单任务成本。前期对话提出的通过率、耗时等数字均为候选目标，不作为未经测试的合同承诺。

## 11. 建议制作顺序

1. 统一视觉样式、登录、导航、首页及三个工作区骨架。
2. 完整打通产品精修的样例交互与助手面板。
3. 完成品牌创作画布及脚本分析／编辑流程。
4. 补素材库、场景卡、模型设置和存储管理展示。
5. 验证三种角色、状态恢复、异常处理与窄屏体验。
6. 在接口与样本就绪后逐项替换模拟能力，验证效果和成本，再进入真实线上试用。

## 12. 待补充资料与决策

不阻塞 Demo 页面制作，可在制作过程中补充：

- 产品名称、品牌 Logo 与正式视觉资产；暂用“创作工作室”。
- 咖啡／酒饮原片及理想成片、格式和文件大小。
- 常用物料类型、具体尺寸及实际导出格式。
- 茶馆真实照片、区域、道具、演员和拍摄限制。
- 单份脚本的定义、时长、题材偏好与最终格式。
- 模型厂商账号、预算、可用地区、数据处理约束。
- 部署区域、域名、存储与备份保留期、支持维护方式。
- 对标数据渠道；独立于 Demo 的示例数据流程后续确定。

## 13. 技术候选来源

以下链接用于技术选型复核，不代表已安装或已决定采购：

- LangGraph：https://github.com/langchain-ai/langgraph
- CopilotKit：https://github.com/CopilotKit/CopilotKit
- LiteLLM：https://github.com/BerriAI/litellm
- Dify：https://github.com/langgenius/dify
- Dify 许可证：https://github.com/langgenius/dify/blob/main/LICENSE

## 14. 可行性评审与范围决策建议

本节用于约束 Demo 范围、验证顺序和后续投入。除明确标注为“已确认”的内容外，以下均为建议决策，不视为合同承诺或已锁定技术方案。

### 14.1 可行性结论

- **可交互 Demo 可行**：在允许示例数据、预制结果和部分模拟状态的前提下，可以完成三条业务流程的可交互演示。
- **真实线上版本需要分阶段**：如果同时要求公网访问、真实客户数据、真实模型调用、稳定批处理、权限隔离、成本计量、备份恢复和持续运维，则工作量已经超出普通 Demo，应按客户试用版或正式产品管理。
- **当前核心阻塞不是界面实现，而是验证目标不够集中**：三个业务板块分别包含较深的专业能力，首版若等量投入，容易出现页面齐全但核心效果未被验证的结果。
- **阶段建议**：当前按“价值验证期”处理，先证明高频任务是否真正节省时间、结果是否可采用，再建设完整平台能力。

### 14.2 建议的三层交付范围

| 层级 | 目标 | 建议范围 | 进入下一层的条件 |
|---|---|---|---|
| A. 可交互原型 | 验证信息架构、操作路径和客户理解 | 三个板块均可操作；允许示例数据、预制结果和模拟任务；所有模拟能力明确标注 | 老板、设计师、编导能分别完成一次端到端任务，并确认工作流方向 |
| B. 真实能力验证 | 验证 AI 效果、人工返工、成本和稳定性 | 优先打通产品精修真实链路；品牌创作和脚本保留浅层演示或少量真实调用 | 真实样本达到双方确认的采用率、返工时间、严重错误和单任务成本门槛 |
| C. 客户试用版 | 支持三人使用真实数据持续工作 | 真实鉴权、私有存储、后台任务、失败恢复、权限、日志、费用、备份和运维 | 连续试用周期内核心任务稳定完成，安全与恢复检查通过，成本可接受 |

未经单独确认，不得把 A 层的模拟能力解释为 B／C 层已经具备的生产能力。若 Demo 部署到公网并上传真实客户数据，登录、鉴权、文件访问、密钥保存和数据删除机制必须提前达到 C 层最低安全要求。

### 14.3 首版主次建议

1. **主链路：产品精修。** 已知每周约 50 张的真实工作量，便于比较处理时间、返工量和单张成本。建议用 20–30 张覆盖不同光线、构图、杯型、瓶型和标签的客户样片验证。
2. **浅层链路：品牌创作。** 首版限制为 2–3 类代表性物料模板；支持独立修改文字、Logo 和产品图，以及少量目标尺寸预览。不建设通用自由画布或完整类 Canva 编辑器。
3. **浅层链路：短剧脚本。** 首版采用人工导入或示例对标资料，完成分析、场景约束、脚本生成、局部修改和 Markdown 导出；不承诺自动采集四个平台数据。

主链路选择最终仍需客户确认。如果客户最希望证明的是品牌物料或脚本产能，应将对应板块提升为唯一真实主链路，同时降低另外两个板块的实现深度，而不是增加总范围。

### 14.4 关键风险与控制方式

| 优先级 | 风险 | 控制方式 |
|---|---|---|
| P0 | 生成式修图改变 Logo、标签文字、杯瓶轮廓或拉花 | 默认先小样后批量；设置保护区；严重失真必须人工拦截；不得宣传百分之百保持商品真实性 |
| P0 | Demo 与生产要求混杂，导致排期和报价失真 | 报价、排期和验收分别对应 A／B／C 层；新增生产能力必须说明替换项或增量成本 |
| P0 | 三条业务线同时做深导致延期 | 每阶段只允许一条真实主链路；其他板块使用浅层流程或预制结果 |
| P0 | 对标平台接入存在授权、反爬、口径和版权风险 | 首版只用用户导入、人工整理或有来源标注的示例数据；正式接入前逐平台确认权限和使用边界 |
| P1 | 品牌画布演变成通用设计软件 | 限定模板、元素类型、尺寸和编辑动作；图层系统、高级排版和印刷生产能力另立项目 |
| P1 | 助手上下文与实际选中对象不同步 | 为图片、画布元素、脚本段落定义稳定 ID 和结构化上下文；每次执行前展示作用范围并让用户确认 |
| P1 | 异步任务重试造成重复执行和重复计费 | 使用任务幂等标识、步骤状态、供应商任务 ID 和结果检查；费用未知时不得伪造估算 |
| P1 | 模型费、存储和流量超出预算 | 分别记录单任务模型费、文件版本数量、存储量和下载流量；达到管理员设置的限制时暂停新任务并提示 |
| P1 | 客户素材、人物照片和模型服务的数据处理风险 | 上线前确认授权、服务地区、第三方模型留存政策、删除期限和备份删除流程；敏感密钥只保存在服务端 |
| P1 | AI 脚本出现相似表达、事实错误或不可拍内容 | 保留来源、差异和人工审核；相似度提示仅作辅助，不作为原创或版权结论 |

### 14.5 建议增加的结果型验收指标

页面和状态验收之外，B／C 层应记录以下指标。具体目标值需在取得真实基线后由双方确认，未经测试不得写入合同承诺。

| 板块／系统 | 最低记录指标 | 建议测试样本 |
|---|---|---|
| 产品精修 | 单张平均处理时间、人工返工时间、结果采用率、严重商品失真数、批量失败率、单张模型成本 | 20–30 张真实样片，覆盖主要拍摄差异 |
| 品牌创作 | 首个可用方案耗时、方案采用率、每份物料人工调整时间、换尺寸后异常数、单任务成本 | 3–5 个真实活动任务、2–3 类常用物料 |
| 短剧脚本 | 初稿耗时、进入实际拍摄比例、场景／道具约束错误数、编导修改时间、单份成本 | 先明确“一个脚本”的定义，再测试不少于 10 个真实选题 |
| 系统稳定性 | 上传失败率、任务失败率、刷新后恢复率、重复执行／重复计费次数、导出成功率 | 覆盖正常、取消、超时、断网、重试和权限不足场景 |

基线采集方式：先由现有团队使用原工作方式完成同类任务，记录耗时、返工和采用情况；再使用工作台完成同一批或难度相近的任务。只有当工作台在效率、质量或成本中至少一项明显改善，且其他项没有不可接受退化时，才进入下一阶段。

### 14.6 阶段闸门与停止清单

#### A → B：从原型进入真实能力验证

- 三类用户均能在不依赖讲解的情况下找到入口并完成各自核心流程。
- 客户明确选择唯一真实主链路，并提供对应真实素材和评价人。
- 模型厂商、预算上限、数据处理约束和输出格式有初步结论。

#### B → C：从能力验证进入客户试用

- 主链路达到双方确认的采用率、返工时间、严重错误和成本门槛。
- 已完成真实鉴权、文件权限、密钥保护、任务幂等、日志和基础恢复验证。
- 明确试用周期、支持范围、问题响应人和停止条件。

#### 当前不要做

- 不同时建设多个智能体编排平台；候选技术只选择满足当前主链路的最小组合。
- 不建设完整通用设计画布、PSD／AI 原生源文件和印刷生产链路。
- 不承诺四个平台自动抓取、全网爆款判断或跨平台指标直接可比。
- 不在真实效果和使用价值尚未验证前投入多租户 SaaS、复杂审批、自训模型、3D 重建或视频生成。
- 不把演示账号、演示数据和预制结果包装成真实线上能力。

### 14.7 Demo 开工前必须拍板的事项

以下三项会直接改变范围、报价和排期，应在进入详细设计或开发前确认：

1. **演示对象与环境**：仅内部展示、客户现场演示，还是公网可访问并保存真实数据。
2. **时间、团队和预算边界**：目标演示日期；产品、设计、前后端和测试投入；模型调用与基础设施预算上限。
3. **唯一真实主链路**：产品精修、品牌创作、短剧脚本三者中，哪一个必须在首版展示真实 AI 效果，其余两个允许模拟到什么程度。

确认后应形成一页《Demo 范围确认单》，列出：真实能力、模拟能力、不包含项、验收样本、结果指标、费用边界、负责人及范围变更规则。新增内容若不增加时间或预算，必须替换掉同等工作量的已排内容。
