# AI 创作工作台｜Demo v0.7 需求文档

> 文档版本：v0.7  
> 更新日期：2026-09-15  
> 使用对象：老板、设计师、编导  
> 产品定位：日咖夜酒与茶馆团队内部使用的轻量 AI 创作工作台

## 1. 本版目标

v0.7 延续 v0.6 的后台任务、持久化和资产库要求，重点优化 AI 表单理解、生成前二次确认、工作台跳转和创作对话框的可读性。系统应根据用户实际选择的素材实时识别内容并填入相应词条，同时保留用户对每个字段和最终提示词的编辑控制权。

本版建立一条强制原则：**创作页负责生产，库页面负责保存、查找、预览和管理。** 创作完成的内容必须自动进入对应的库；库页面不再重复出现创作模式按钮或完整机器人对话面板。

三个核心板块保持不变：

1. 产品精修：处理咖啡、酒等产品照片，支持单张、批量和模版化修图。
2. 品牌创作：基于品牌专属素材生成海报、活动物料和文创延展设计。
3. 短剧创作：管理真实茶馆场景资产，生成并编辑剧本，再从已保存剧本发起视频生成。

本版必须同时解决：

- 功能入口过多、层级含混的问题。
- 平台资产库与业务资产库职责重叠、命名和引用边界不清的问题。
- 不同业务素材可能互相引用的问题。
- 智能体与工作步骤脱节的问题。
- 生成后不知道保存在哪里、下一步做什么的问题。
- 长任务反馈慢、错误反馈不清晰的问题。
- 品牌案例仍停留在“素材创作”页，没有真正归入产品库的问题。
- 剧本案例仍停留在“资产库”页，没有真正归入剧本库的问题。
- 图库中重复出现“添加图片、批量修图、修图模版”和修图助手的问题。
- 批量选图仅靠边框或轻微变色，用户无法快速确认选中了哪些图片的问题。

### 1.1 v0.7 相对 v0.6 的关键变更

1. 产品精修表单不再使用固定预设；选择或上传图片后，由视觉模型实时识别并填入尺寸、场景、装饰、产品类型、保留项和精修要求。
2. AI 自动填写的所有词条都允许用户修改、清空、补充和锁定，后续识别不得覆盖已锁定或已人工修改字段。
3. 点击“生成精修计划”后进入同尺寸的第二阶段对话框，展示并允许编辑最终提示词，再由用户正式提交图片生成。
4. 工作台顶部三个模块入口、待确认任务、继续创作卡片及箭头区域全部可点击并进入对应工作台或任务详情。
5. 品牌创作和短剧创作对话框重新设定宽高、内容密度和字号，避免大面积无效留白与小字难读。
6. 建立统一字号、输入框和对话框响应式规范，桌面端正文不小于 16px，辅助文字不小于 14px。
7. v0.6 已列出的后台任务、真实归档、历史恢复和平台资产库未完成项全部继续保留，不因本次界面优化而移出范围。

## 2. 项目现状检查与调整结论

根据当前项目页面与交互检查，v0.6 需要做以下调整：

| 当前状态 | 问题 | v0.6 调整 |
| --- | --- | --- |
| 产品精修顶部直接展示“添加图片、批量修图、修图模版” | 三个操作与资产管理处于同一层级 | 顶部改为“一键修图、图库”；三个操作降为“一键修图”内的模式选择 |
| 品牌创作为“素材上传、产品案例、效果渲染” | 名称不符合实际工作流，产出归属不明确 | 改为“素材创作、产品库”；生成结果自动进入产品库 |
| 品牌素材选择器可以同时读取品牌和精修素材 | 违反业务隔离要求 | 只允许查询和引用品牌资产 |
| 短剧已有“资产库、剧本库、视频生成”，但视频仅为占位 | 流程未真正串联 | 建立“资产→剧本→视频任务”的关联与前置校验 |
| 剧本主要保存在浏览器本地存储 | 无法可靠在线保存、跨设备使用或做版本恢复 | 改为服务端持久化，支持自动保存、手动保存与版本记录 |
| 左侧已存在一级“资产库” | 功能需要保留，但必须避免与短剧资产库混淆和跨模块误用 | 保留一级“资产库”作为平台统一索引；板块内资产库继续承担具体业务管理 |
| 已有统一右侧助手和任务进度样式 | 在纯库页面常驻会挤压空间 | 创作/编辑时显示助手，纯库页面默认隐藏；能力和素材范围随板块切换 |
| 品牌创作页底部直接展示案例瀑布流 | 创作输入和历史成品混排，页面过长 | 案例整体迁入产品库；创作页只展示本次任务结果 |
| 短剧资产库底部直接展示剧本案例 | 资产与文字成果混为一类 | 剧本案例整体迁入剧本库；资产库只管理创作依据 |
| 产品图库仍显示三项修图按钮和右侧助手 | 浏览结果时界面拥挤，也容易误以为正在创作 | 图库改为纯资产管理页，移除修图工作条和机器人对话 |
| 批量素材选择缺少强提示 | 用户难以判断是否选中，容易漏选或重复选 | 使用复选框、品牌色描边、遮罩、编号、计数五重反馈 |
| 生成时工作区被当前任务占用 | 用户必须等待，无法马上开始下一项工作 | 提交成功即转后台运行，界面无刷新闪烁地重置为新任务 |
| 页面刷新后回到初始案例 | 生产记录只存在组件状态或浏览器缓存 | 任务与作品保存到服务端，刷新后按时间恢复历史 |
| 任务是否卡住不直观 | 只有笼统加载状态，没有百分比和更新时间 | 业务库卡片显示阶段化真实进度、百分比、已耗时和最后心跳 |
| 素材横向小窗只适合少量内容 | 后续素材变多后难以浏览 | 支持鼠标滚轮驱动横向滚动，并增加搜索、分类与虚拟化 |
| 完成图片只在所属业务库出现 | 老板无法集中查看全部成品 | 完成后同步建立平台资产库索引，保持一份文件、两处入口 |

### 2.1 2026-09-15 实际验证基线

本节记录当前代码已经验证到的真实状态，后续不得仅依据按钮存在或接口返回成功判定需求完成。

#### 已完成并验证

- 产品精修、品牌创作、短剧编导三个功能入口和板块页签已存在。
- 产品精修、品牌创作、短剧编导 API 均能返回 `202 Accepted`。
- 生图接口能够返回 `200 OK` 和真实 `b64_json` 数据。
- 品牌、精修、短剧三个素材选择器已经做基础隔离。
- 品牌案例已迁入产品库。
- 短剧案例已迁入剧本库。
- 批量选图已有复选框、选择编号和选择计数。
- 当前项目构建通过，自动化测试 8/8 通过。

上述内容代表“基础调用或界面存在”，不等于完整生产闭环已交付。

#### 尚未完成，必须纳入 v0.6

| 编号 | 未完成项 | 当前风险 | v0.6 目标 | 优先级 |
| --- | --- | --- | --- | --- |
| G-01 | 剧本仍保存在浏览器 `localStorage` | 刷新、换设备或清缓存后可能丢失，无法恢复版本 | 服务端数据库持久化、自动保存、版本历史和恢复 | P0 |
| G-02 | 生成图片未自动写入 `storage/generated/{门店}/{业务}/ai-temp` | API 成功但素材库没有真实文件 | 下载模型结果、校验并按分级目录归档 | P0 |
| G-03 | 缺少 `manifest.json`、缩略图、到期清理和 ZIP 下载 | 文件不可追溯、图库慢、存储会持续增长 | 完成清单、缩略图、生命周期和批量下载 | P0 |
| G-04 | 视频生成只有入口，没有剧本版本到视频任务的链路 | 用户无法从已保存剧本真正发起任务 | 版本选择、分镜确认、后台任务和状态恢复 | P1 |
| G-05 | 产品精修真实图片归档、单张失败重试、批量任务状态未完全接通 | 批量任务容易整体失败或刷新丢失 | 逐项状态、失败隔离、任务恢复与自动归档 | P0 |
| G-06 | 一级资产库尚未完全按本版重新定义 | 可能和业务库重复或造成跨模块引用 | 保留入口，改为统一索引、集中管理和权限过滤 | P0 |
| G-07 | 账号角色权限、操作日志和任务中心未完成 | 密钥、删除和跨业务访问缺少治理 | 完成老板/设计师/编导权限、审计和任务中心 | P0 |
| G-08 | 已生成结果刷新页面后不能可靠恢复 | 用户无法保留生产历史 | 结果与任务写入服务端，刷新、重登、重启均可恢复 | P0 |

### 2.2 完成判定原则

每项能力必须同时满足“页面可操作、API 可调用、服务端有持久记录、刷新后可恢复、异常可处理、自动化测试通过”六个条件，才可标记完成。仅有 UI、仅返回 HTTP 成功码或仅拿到模型 Base64 数据，都只能标记为部分完成。

## 3. 信息架构

### 3.1 左侧主导航

- 工作台：首页、待办任务、近期产出、失败任务提醒。
- 产品精修。
- 品牌创作。
- 短剧创作。
- 资产库：跨门店、跨业务的统一成品索引、原始素材与存储概览。
- 底部：任务中心、通知、存储用量、账号与系统设置。

业务素材仍在所属板块内管理。左侧一级“资产库”必须保留，它是平台统一索引入口，不是可任意混用的上传池：可以集中搜索、上传、查看和下载，但引用时仍需校验目标业务、门店、资产类型和用户权限。

### 3.2 板块内导航

| 板块 | 一级按钮/页签 |
| --- | --- |
| 产品精修 | 一键修图、图库 |
| 品牌创作 | 素材创作、产品库 |
| 短剧创作 | 资产库、剧本库、视频生成 |

页签位于页面标题下方并保持固定位置。切换页签时保存当前草稿、筛选和滚动状态，避免误操作导致内容丢失。

### 3.3 页面通用布局

创作页面在桌面端使用三段式布局：

- 左侧：全局导航。
- 中间：当前任务与内容工作区。
- 右侧：当前板块的 AI 助手、任务状态和下一步操作。

当屏幕宽度不足时，AI 助手移到主工作区下方，以抽屉或可折叠面板呈现。助手不能遮挡上传、保存、确认等核心按钮。

库页面使用两段式布局：左侧导航 + 全宽内容区。默认不显示右侧 AI 对话栏，以便图库、产品库和剧本库获得更大的浏览与编辑空间。只有用户点击具体作品的“继续创作/AI 修改”时，才进入对应创作页或打开轻量编辑抽屉。

### 3.4 提交即离场的统一任务体验

三个功能板块统一执行以下交互：

1. 用户点击“确认并提交”后，按钮立即显示提交中并防止重复点击。
2. 服务端在 1 秒内返回永久任务 ID 和 `queued` 状态；只有收到任务 ID 才视为提交成功。
3. 页面右上角提示“任务已提交，可继续创建”，并提供“查看任务”快捷入口。
4. 当前输入、所选素材、Skill 计划和模型参数保存为任务快照。
5. 创作区在 300ms 内切换成干净的新任务界面，不执行浏览器整页刷新，也不等待模型结果。
6. 上一个任务立即出现在所属业务库首位，同时出现在全局任务中心。
7. 用户可以连续提交新任务；每个任务独立运行，受服务端并发限制与队列调度控制。

若服务端没有成功返回任务 ID，创作区不得清空，必须保留全部输入供用户重试。重置后提供 5 秒“恢复上一任务输入”入口，但恢复只复制参数，不撤销已经提交的任务。

### 3.5 任务进入各业务库的规则

| 提交来源 | 任务立即出现的位置 | 完成产出位置 |
| --- | --- | --- |
| 产品精修 | 产品精修 / 图库 / 处理中 | 产品精修 / 图库 / AI 临时生成 |
| 品牌素材创作 | 品牌创作 / 产品库 / 处理中 | 品牌创作 / 产品库 / AI 临时生成 |
| 短剧生成 | 短剧创作 / 剧本库 / 生成中 | 短剧创作 / 剧本库 |
| 视频生成 | 短剧创作 / 视频生成 / 任务中 | 短剧创作 / 视频生成 / 任务结果 |

库页面不能只展示成功成品，也必须展示排队、处理中、需要处理、失败和已取消的历史任务。

### 3.6 进度与卡住识别

任务卡片固定显示：

- 任务名称、缩略图或输入素材预览。
- 当前百分比和进度条。
- 当前阶段，例如“等待模型”“生成第 2/4 张”“保存结果”“生成缩略图”。
- 已耗时、创建人、所用模型。
- 最后一次状态更新时间。
- 查看详情、取消、重试和删除操作。

百分比不能由前端按时间随意增长。优先使用模型提供的真实进度；模型不提供细粒度进度时，按可验证阶段加权：

| 阶段 | 百分比区间 |
| --- | --- |
| 已受理/排队 | 0%—5% |
| Skill 解析与计划固化 | 5%—15% |
| 素材预处理与上传模型 | 15%—25% |
| 模型运行 | 25%—85% |
| 结果下载与校验 | 85%—93% |
| 文件归档与缩略图 | 93%—99% |
| 数据库完成并可访问 | 100% |

模型运行阶段没有真实子进度时，固定显示 25% 或“模型处理中”，不得制造匀速增长的假进度。只有文件与数据库均写入成功后才能显示 100%。

卡住判定：

- 正常：最近心跳在阈值内，显示绿色/中性色状态。
- 可能较慢：超过该模型历史 P95 时长，显示“耗时较长，仍在运行”。
- 疑似卡住：连续 2 个心跳周期无更新，显示橙色提醒并允许刷新状态。
- 已超时：超过任务超时上限，由服务端标记失败，释放并发位并提供重试。

前端刷新不能改变任务状态；状态必须来自服务端任务记录。优先使用 SSE/WebSocket 推送，断线时自动切换为 3—5 秒轮询，恢复连接后停止轮询。

### 3.7 历史记录与删除

- 历史任务、提示词、输入素材、产出、状态和错误信息均保存在服务端。
- 刷新页面、关闭浏览器、重新登录或换设备后均可恢复。
- 默认按最新创建时间排序，支持状态、用户、门店、模型和日期筛选。
- 点击“再次创建”可复制历史参数到新的创作区，不覆盖原任务。
- 运行中的任务可取消但不可直接删除；取消确认后再进入历史状态。
- 已完成、失败和已取消任务可删除至回收站，默认保留 24 小时后清理。
- 删除历史任务时明确提示是否同时删除临时产出；用户可选择“仅删除记录”或“记录和临时文件一起删除”。
- 被其他版本或正式成品引用的文件不可直接物理删除，系统需提示引用关系。
- 老板可查看全部记录；设计师和编导按角色权限查看及删除自己的业务记录。

### 3.8 引用素材小窗滚动规范

图一所示素材选择区域需从固定横排升级为可扩展选择器：

- 鼠标位于素材条上时，垂直滚轮可驱动横向滚动；触控板保持自然横向滚动。
- 滚到素材条起点或终点后，将滚轮事件交还页面，不能把用户困在小窗内。
- 显示可拖动的细滚动条，并在左右边缘显示渐隐遮罩，提示仍有更多内容。
- 支持 Shift + 滚轮、左右方向键和拖拽浏览。
- 素材超过 20 个时提供搜索与分类；超过 100 个时使用虚拟列表或分页。
- 滚动过程中不触发误选；点击卡片后仍显示明确勾选态。
- 重新打开窗口时恢复上一次滚动位置、筛选和已选素材。
- 移动端改为横向触摸滑动或底部全屏素材抽屉。

## 4. 产品精修

### 4.1 顶部页签

#### 一键修图

默认进入此页。页面顶部先让用户选择一种工作模式：

1. 上传图片：适合一张或少量产品图的精细处理。
2. 批量修图：一次上传或从图库选择多张照片，使用相同规则批量处理。
3. 修图模版：先选择已保存的修图参数，再选择单张或多张图片执行。

三个模式使用紧凑的分段选择器或卡片，不作为新的页面跳转。选中后只展示当前模式需要的控件。

#### 图库

只展示产品精修板块的资产，分类包括：

- 原始产品图。
- 修图参考图。
- 精修结果。
- 修图模版。

支持搜索、门店、产品类型、创建时间、任务状态和创建人筛选。支持查看原图与结果的关联、版本对比、下载、继续修图和归档。

图库是纯浏览与管理页面，明确移除截图中标注的以下内容：

- “添加图片、批量修图、修图模版”工作条。
- 右侧完整“精修助手”对话面板及参数表单。
- “生成修图计划”等创作按钮。

图库顶部只保留搜索、分类筛选、排序、视图切换和“去一键修图”一个主按钮。点击某张图片的“继续修图”时，系统携带该图片跳转至“一键修图”，而不是在图库原地展开机器人。

### 4.2 一键修图工作流

1. 用户选择上传图片、批量修图或修图模版。
2. 上传本地文件，或从当前板块的图库选择素材。
3. 系统立即完成格式、尺寸、数量和文件损坏检查。
4. 用户用自然语言描述要求，也可点击快捷标签，例如“背景干净”“杯壁去污”“冰块通透”“保持品牌标签”。
5. 修图助手读取图片、用户要求和所选模版，生成结构化修图计划。
6. 用户确认计划；批量任务需提示应用范围和预计消耗。
7. 系统提交任务并持续显示阶段：上传中、分析中、等待确认、排队中、处理中、质检中、已完成或失败。
8. 结果以对比视图展示，默认自动保存到产品精修“图库—精修结果”。
9. 用户可下载、继续修改、回退版本或将当前参数保存为修图模版。

### 4.3 批量修图规则

- 首版建议单批最多 50 张，超过限制时引导拆批。
- 上传后先显示缩略图队列，允许移除、补传、重新排序。
- 可对全批使用同一要求，也可对单张追加特殊说明。
- 执行前明确显示图片数量、所用模版、输出尺寸和费用预估。
- 单张失败不应导致整批失败；支持只重试失败项。
- 批量结果按任务分组保存，并保留每张图的原图、结果、版本和错误信息。

#### 批量素材选择反馈

每张可选素材必须提供清晰、可访问的选择状态：

- 卡片右上角始终显示复选框；未选为空心，已选显示深咖底白色勾。
- 已选卡片显示 2px 品牌色描边和轻微半透明遮罩，不仅依赖颜色变化。
- 勾选后显示选择顺序号，方便批量任务确认顺序。
- 单击整张卡片即可选中/取消，复选框同步变化。
- 键盘用户可用空格切换，焦点态与选中态不可混淆。
- 页面底部出现固定操作栏：“已选 N 张｜全选本页｜清空｜使用选中素材”。
- 切换分页或筛选时保留已选项，并显示“本页 N 张、共选 M 张”。
- 批量上限前 5 张开始提示剩余数量，达到上限后禁用未选卡片并解释原因。
- 删除、取消选择立即反馈，不等待服务器请求完成。

### 4.4 修图助手

助手区域展示：

- 当前已选图片数量和素材来源。
- 当前修图意图与必须保留项。
- AI 生成的修图计划。
- 可直接修改的参数摘要。
- 明确的“确认并开始修图”主按钮。
- 任务状态、错误原因和重试入口。

助手只允许调用产品精修 Skill、产品精修素材和当前任务上下文，不可读取品牌或短剧资产。

### 4.5 素材识别与动态预设

图一右侧“尺寸与比例、场景、装饰、精修要求”等内容必须由当前选中素材驱动，不能长期保留与图片无关的固定默认值。

#### 触发时机

- 用户上传一张新图片。
- 用户从图库切换选中图片。
- 批量任务增删图片，且主参考图发生变化。
- 用户主动点击“重新识别”。

选择变化后立即显示“正在识别素材”，并在目标 1—3 秒内回填结果；识别不得阻塞图片预览和其他页面操作。快速连续切换素材时取消旧请求，只采用最后一次选择的结果。

#### 自动识别字段

| 字段 | 自动识别/推断内容 | 展示形式 |
| --- | --- | --- |
| 产品类型 | 咖啡、鸡尾酒、瓶装酒、茶饮、器皿等 | 单选词条，可改 |
| 原图尺寸与比例 | 像素尺寸、横竖方向、原始比例、格式 | 只读原值 + 可选输出设置 |
| 场景 | 咖啡店、吧台、茶馆、纯色棚拍等 | 推荐词条 + 自定义输入 |
| 主体 | 杯子、瓶身、Logo、标签、液体、冰块等 | 多选词条 |
| 装饰/道具 | 花材、咖啡豆、茶具、桌面道具等 | 多选词条，可删除 |
| 必须保留 | 产品轮廓、品牌文字、包装结构、杯型、颜色等 | 默认勾选，用户确认 |
| 建议精修项 | 去污点、校正曝光、提升通透、压反光、清理背景等 | 建议词条，可选用 |
| 风险项 | 文字可能失真、反光区域过曝、主体遮挡等 | 警告词条，不自动隐藏 |

识别结果应携带来源与置信度。低置信度词条使用“AI 推测”标记，不能伪装成确定事实。

#### 用户编辑保护

- AI 首次识别可以自动填写空字段。
- 用户编辑后的字段标记为“已人工修改”，后续自动识别不得覆盖。
- 每个字段提供锁定/解锁；锁定后切换参考图也保持内容。
- 用户可点击单字段“使用 AI 建议”恢复最新识别结果。
- “全部重新识别”必须先提示会替换哪些未锁定字段。
- 自动值、用户修改值和最终提交值均写入任务快照，便于回溯。
- 批量修图默认以第一张主参考图生成公共建议，同时提示哪些图片存在明显场景差异；用户可为异常图片单独覆盖。

### 4.6 两阶段精修对话框

产品精修使用两个尺寸一致、位置一致的面板状态，避免页面跳动：

#### 第一步：需求识别框

- 展示素材预览、动态词条、输出尺寸和用户补充要求。
- 主按钮为“生成精修计划”。
- 该步骤只调用 Skill/文本模型生成计划，不调用图片模型。

#### 第二步：提示词确认框

点击“生成精修计划”后，在原面板位置切换到同宽同高的“确认生成提示词”对话框：

- 顶部显示步骤 `2/2`、所选素材缩略图和返回按钮。
- 展示结构化修图计划、最终正向提示词、禁止修改项、输出尺寸、格式和预计张数。
- 最终提示词使用可编辑多行文本框，用户可直接修改。
- 支持“采用 AI 原稿”“重新生成计划”“恢复上次修改”和“返回调整参数”。
- 用户修改提示词后明确标记“已人工修改”，并保存草稿。
- 固定底部操作区：次按钮“返回修改”，主按钮“确认并生成图片”。
- 只有点击最终主按钮才调用真实图片模型并创建后台任务。
- 提交前显示模型、预计费用/次数、输出数量和保存位置。
- 如果提示词为空、素材失效或必须保留项冲突，禁止提交并准确定位问题。

两个面板切换采用 150—200ms 淡入淡出，不改变页面整体布局和滚动位置。关闭后再次打开应恢复当前步骤与未提交编辑内容。

## 5. 品牌创作

### 5.1 顶部页签

#### 素材创作

用于选择品牌素材并调用生图机器人。页面内容按顺序为：

1. 选择创作类型：海报、社交媒体物料、菜单视觉、包装延展、文创周边等。
2. 上传素材，或从品牌资产中选择 Logo、产品图、字体、色彩、历史作品和参考图。
3. 输入用途、尺寸、文案、风格、必须出现和禁止出现的元素。
4. 生图机器人生成创意方向与结构化提示词。
5. 用户确认方向后生成图片。
6. 页面只展示本次任务生成的候选结果，不展示历史案例瀑布流。
7. 用户选择保存或任务完成后，结果自动同步到“产品库”。

品牌 VI 手册尚未接入时，页面显示“VI 约束未配置”，但仍允许使用已上传素材创作。后续接入 VI 后，助手自动读取色彩、字体、Logo 安全区和禁用规则。

#### 产品库

此处的“产品”指品牌创作成品，不等同于产品精修图库。页面副标题固定显示“品牌成品与设计方案”，降低歧义。

分类包括：

- 草稿。
- 生成中。
- 待确认。
- 已通过。
- 已归档。

支持按物料类型、门店、活动、尺寸、创建人和日期筛选。每个成品保留使用素材、提示词、模型、版本和生成任务的关联。

截图一红框中的“茶山云雾香器”等品牌案例及其图片应全部归入产品库，不再出现在素材创作页底部。产品库卡片应展示封面、名称、类型、版本数、状态、更新时间；点击后打开详情页查看全部方案图和版本。

产品库默认不显示生图机器人的完整对话面板。卡片上的“继续创作”会携带原素材、提示词和版本跳转回“素材创作”，避免同一页面同时承担浏览与生成。

### 5.2 生图机器人交互

- 默认先输出 1—3 个可选择的创意方向，不直接消耗生成额度。
- 每个方向展示构图、色彩、文案位置、引用素材和风险提醒。
- 用户确认一个方向后才生成图片。
- 结果页支持继续调整、生成变体、下载和标记通过。
- 每次调整生成新版本，不覆盖旧版本。
- 页面必须清楚标注“已自动保存到产品库”。

### 5.3 品牌资产边界

品牌创作只能上传、查看和引用 `brand` 资产。产品精修结果不能直接出现在品牌素材选择器中。如确需复用，后续可增加由用户主动触发的“复制到品牌资产”功能；v0.7 Demo 默认不开放跨模块复制。

### 5.4 品牌创作对话框尺寸

图三所示品牌创作区域不能依靠大面积空白撑满页面。桌面端按内容自适应，建议：

- 工作区最大宽度 1280px，左右留白对称；超宽屏不无限拉伸文本行。
- 输入与计划面板宽度占可用区 100%，最小高度 320px，内容增长时自然扩展。
- 需求文本框默认高度 140—180px，支持拖动调整，最大高度不超过视口的 45%。
- 品牌素材条、输入框、创意方向和生成结果之间使用 16—24px 统一间距。
- 生成创意方向后在输入框下方出现结构化方案卡，不把结果放到屏幕最底部空白区域。
- 底部主操作在内容区内保持可见，不能横跨整个超宽页面形成过长按钮。
- 窄屏时改为单列，按钮宽度 100%；桌面端按钮宽度随文字，最小 120px。

## 6. 短剧创作

### 6.1 资产库

用于管理短剧专属素材，建议分类：

- 场景：茶馆外景、门头、前厅、包间、吧台、走廊等。
- 机位：可拍角度、镜头移动范围、光线和噪声情况。
- 角色：人物设定、服装和关系，不强制存储真人敏感信息。
- 道具：茶具、桌椅、菜单、产品和可移动物件。
- 拍摄约束：营业时间、禁拍区域、容纳人数、不可虚构内容。
- 对标资料：获得合法使用权的数据、链接、摘要和分析结果。

资产卡需包含名称、类别、所在区域、可用状态、更新时间和备注。场景图片应支持多视角归组为同一“场景卡”。

页面提供“用已选资产写剧本”主按钮。点击后将当前场景与约束带入编导助手，而不是让用户重复描述。

资产库页面不得展示完整剧本案例卡。截图二红框中的《00后掌柜整顿老茶馆》《穿成茶馆老板后我爆单了》《前任在我茶馆办婚礼》等内容应迁入剧本库。资产库只回答“有哪些创作依据可用”，不承担“有哪些剧本成果”的展示。

### 6.2 编导助手

编导助手不再作为三个库页面永久占位的右侧对话栏，而按任务需要出现：

- 在资产库：用户选好资产并点击“用已选资产写剧本”后，进入新建剧本流程并打开助手。
- 在剧本库：用户新建剧本或打开某个剧本编辑器后，助手才出现，用于续写、改写、压缩时长和检查场景可拍性。
- 在视频生成：用户选择剧本版本后，助手以“分镜规划”步骤出现，不展示无关闲聊。

脚本生成流程：

1. 选择真实场景资产。
2. 输入题材、平台、受众、时长、集数、角色数和创作目的。
3. 如已录入合法对标资料，助手先总结其结构、冲突、节奏和吸引点，不直接复制原剧情或台词。
4. 生成故事方向和可拍性检查。
5. 用户确认后生成完整脚本。
6. 脚本自动保存到剧本库并进入可编辑状态。

所有生成内容必须绑定现有茶馆场景，不得默认创建现实中不存在的房间、设备或大规模场面。缺少必要资产时，先给出缺失提示和替代方案。

### 6.3 剧本库

剧本库采用“列表/卡片 + 编辑器”的结构，支持：

- 新建、重命名、复制、归档和删除至回收站。
- 正文直接编辑。
- 自动保存，并显示“保存中、已保存、保存失败”。
- 手动保存重要版本。
- 版本历史、恢复和差异查看。
- 剧名、题材、集数、预计时长、状态、创建人、更新时间等元数据。
- 标记草稿、待审、已通过和可生成视频。
- AI 对选中段落进行改写，而不是每次重写全文。

剧本库首页展示所有案例和用户生成剧本，包括截图二红框中的现有三个案例。首页默认无大段机器人输入框，只保留搜索、筛选、状态、排序、“新建剧本”和剧本卡片。打开卡片后进入编辑器，AI 编辑助手才在右侧出现。

建议编辑器结构包括：故事梗概、人物小传、分集大纲、分场脚本、对白、动作、场景资产引用和拍摄备注。

剧本必须保存至服务端数据库，浏览器本地存储只能作为临时离线草稿，不可作为正式数据源。

### 6.4 视频生成

视频生成只能选择剧本库中“已保存”的剧本；默认优先显示“已通过/可生成视频”的版本。

工作流：

1. 选择剧本及具体版本。
2. 选择要生成的集数或场次。
3. 系统校验关联场景、角色、道具和镜头描述是否完整。
4. 编导助手拆解分镜、画面提示词、对白/旁白和时长。
5. 用户确认生成范围、比例、清晰度、模型和费用预估。
6. 提交视频任务，显示排队、生成、合成、失败或完成状态。
7. 结果按剧本与场次归档，可下载或重试失败片段。

若 Demo 环境尚未接入视频模型 API，仍需完成剧本选择、前置校验、分镜预览和任务状态模拟，并明确标注“演示任务，未产生真实视频”，不得伪装成功。

### 6.5 短剧创作对话框尺寸

图四所示脚本输入区应从“窄输入框 + 超宽按钮 + 大面积空白”改为清晰的写作工作区：

- 桌面端内容最大宽度 1280px，主体输入与计划区宽度 100%，左右内边距至少 24px。
- 剧本需求输入框默认高度 160px，最小 120px，可向下扩展至 360px。
- “生成脚本大纲”按钮不超过内容区宽度的 320px；桌面端靠右或与辅助操作并排，不使用整页超宽按钮。
- 大纲生成后，在同一区域替换或展开为可编辑大纲面板，默认高度 360—520px，内部允许滚动。
- 大纲字体不小于 16px，行高不小于 1.7；场次标题、人物、对白和动作使用清晰层级。
- 资产缩略图行支持 v0.6 定义的鼠标滚轮浏览，选中状态必须明显。
- 进入完整剧本编辑器后保留当前素材、需求和大纲，不要求用户重新输入。

## 7. 资产隔离与数据规则

### 7.1 强制空间字段

所有资产必须带有不可为空的 `asset_space`：

- `retouch`：产品精修。
- `brand`：品牌创作。
- `script`：短剧资产。

上传入口自动写入当前板块的 `asset_space`，普通用户不能手动修改。资产选择 API 必须由服务端根据登录用户、租户和当前板块过滤，不能只依赖前端隐藏。

### 7.2 产出归档规则

| 产出 | 自动保存位置 |
| --- | --- |
| 精修图片 | 产品精修 / 图库 / 精修结果 |
| 修图参数 | 产品精修 / 图库 / 修图模版 |
| 品牌生成图片 | 品牌创作 / 产品库 |
| 生成剧本 | 短剧创作 / 剧本库 |
| 视频及分镜 | 短剧创作 / 视频生成 / 任务结果 |

每份产出记录来源素材、创建人、创建时间、所用 Skill、模型、提示词/参数、任务 ID 和版本号，便于复现与追责。

### 7.3 权限规则

- 老板：查看所有板块、审批成品、配置模型、查看用量。
- 设计师：读写产品精修和品牌创作；默认只读短剧成品，不可配置模型密钥。
- 编导：读写短剧资产、剧本和视频任务；默认只读品牌成品。
- 所有模型密钥仅保存在服务端，不下发浏览器。
- 上传、下载、删除、生成和跨版本恢复均记录操作日志。

## 8. 智能体与多模型调用

每个板块绑定独立智能体和 Skill：

| 智能体 | 职责 | 可访问数据 |
| --- | --- | --- |
| 修图助手 | 理解修图要求、生成修图计划、调用图像编辑模型、质检 | 仅产品精修任务和 `retouch` 资产 |
| 生图机器人 | 生成创意方向、结构化提示词和品牌图片 | 仅品牌任务、品牌规则和 `brand` 资产 |
| 编导助手 | 对标结构分析、剧本生成编辑、分镜拆解 | 仅短剧任务和 `script` 资产 |

系统采用统一模型网关，智能体不直接绑定某个厂商。每个能力使用逻辑路由，例如 `text.reasoning`、`image.edit`、`image.generate`、`video.generate`。管理员可配置主模型、备用模型、超时、重试和费用上限。

调用链统一为：自然语言需求 → Skill 生成结构化计划 → 用户确认 → 模型执行 → 结果质检 → 保存到所属资产库。

失败切换备用模型时，系统应保留原任务上下文并告知用户，不应让用户重新上传素材或重写要求。

### 8.1 真实图片生成调用链

产品精修与品牌创作必须执行以下链路：

1. 用户选择门店、业务功能、输入素材和自然语言要求。
2. 服务端验证账号权限、素材归属、文件格式与模型配置。
3. 对应智能体加载已注册 Skill；Skill 输出结构化提示词、负面约束、尺寸、质量、参考图策略和执行参数。
4. 页面先展示可读的执行计划，用户确认后创建 `AgentTask`。
5. 模型网关依据能力路由选择真实图片编辑或图片生成模型。
6. 服务端调用模型 API；密钥不得发送到浏览器。
7. 对响应进行文件类型、尺寸、完整性和安全检查。
8. 把真实图片写入当前门店、当前业务素材库的“AI 临时生成”子文件夹，同时生成缩略图和清单记录。
9. 数据库记录文件相对路径、提示词、Skill 版本、模型、费用、来源素材和到期时间。
10. 前端收到可访问的受控 URL，显示真实结果，并提供下载、批量下载、继续编辑和删除。

任何一步失败都必须将任务标记为 `failed` 并显示具体阶段。不得用静态案例图、预置图片、Base64 占位内容或旧任务结果替代本次生成结果。

### 8.2 Skill 输出契约

Skill 的结果使用统一 JSON 结构，至少包含：

```json
{
  "skill_id": "image-edit-agent",
  "skill_version": "x.y.z",
  "task_type": "image.edit",
  "prompt": "发送给模型的最终提示词",
  "negative_constraints": ["不得修改品牌文字", "不得新增产品"],
  "input_asset_ids": ["asset-id"],
  "output": {
    "width": 2048,
    "height": 2048,
    "format": "png",
    "count": 1
  },
  "requires_confirmation": true
}
```

服务端必须校验该结构，禁止 Skill 自行拼接任意文件路径、读取其他素材空间或直接暴露模型密钥。提示词原文和 Skill 版本要随任务保存，确保结果可追溯。

### 8.3 模型网关要求

- 能力路由至少支持 `image.edit` 与 `image.generate`。
- 每项能力可配置主模型和一个备用模型，但自动切换前不得重复扣费；无法判断扣费状态时先提示管理员。
- 模型适配器统一处理鉴权、超时、重试、结果下载、错误码和用量。
- 创建任务前验证 API 是否可用；未配置时按钮显示“模型未配置”，不能进入假生成流程。
- 模型返回远程临时 URL 时，服务端应在 URL 失效前下载并写入本地临时素材库。
- 保存模型请求 ID，便于排错和供应商账单核对。
- 管理员可查看当前路由、连通状态、最近失败率和单任务费用；普通用户看不到密钥。

## 9. 临时素材库与文件存储

### 9.1 产品定义

“AI 临时生成”是每个业务素材库内的系统子文件夹。它用于承接模型新生成的文件，让用户在一定期限内预览、下载或继续编辑；它不是永久档案，也不是 Git 版本资产。

用户进入素材库时，可看到该子文件夹及未过期文件。文件卡片显示“临时”、生成时间、到期时间、门店、创建人和下载状态。

### 9.2 门店与业务归档规则

保存路径先由用户当前工作空间决定，再由业务类型决定：

| 当前门店 | 业务 | 页面归属 | 临时保存位置 |
| --- | --- | --- | --- |
| 日咖夜酒 | 产品精修 | 产品精修 / 图库 | 日咖夜酒 / 产品精修 / AI 临时生成 |
| 日咖夜酒 | 品牌创作 | 品牌创作 / 产品库 | 日咖夜酒 / 品牌创作 / AI 临时生成 |
| 茶馆 | 产品精修 | 产品精修 / 图库 | 茶馆 / 产品精修 / AI 临时生成 |
| 茶馆 | 品牌创作 | 品牌创作 / 产品库 | 茶馆 / 品牌创作 / AI 临时生成 |

咖啡、酒类产品图在默认情况下归入“日咖夜酒”，但提交前页面必须显示当前门店，并允许有权限的用户切换。系统不能仅靠提示词猜测保存位置。

短剧生成的分镜参考图或视频封面归入“茶馆 / 短剧创作 / AI 临时生成”；剧本文本仍保存到数据库与剧本库，不作为图片文件处理。

### 9.3 平台资产库功能与同步机制

平台资产库是对所有门店、业务原始素材和生成成品的统一索引。它保留现有一级入口，但不再复制第二份图片文件。图片生成完成并成功写入业务库后，系统在同一事务或可靠事件中创建 `MasterAssetIndex` 记录：

- `asset_id`：指向业务库中唯一的原始资产记录。
- `tenant_id`、`store_id`、`asset_space`：保留原门店和业务归属。
- `source_task_id`：关联生成任务。
- `sync_status`：`pending`、`synced` 或 `failed`。
- `visibility`：按角色和业务权限计算。
- `created_at`、`expires_at`：与源资产生命周期一致。

同步规则：

1. 任务生成期间只在业务库和任务中心展示，不进入平台资产库的“已完成”结果区。
2. 图片校验与业务库保存成功后，自动同步索引到平台资产库，无需用户再次上传。
3. 平台资产库展示同一文件的缩略图和元数据，不产生第二份原图，不增加重复存储。
4. 同步失败不把图片任务标记成生成失败；卡片显示“图片已生成，资产库同步失败”，后台自动重试。
5. 从业务库删除源图片时，平台资产库索引同步进入回收站；从平台资产库删除时必须明确是仅移除索引还是删除源文件，普通用户默认只能移除索引。
6. 临时图片到期后，平台资产库同步隐藏或标记已过期，不能留下失效缩略图。
7. 平台资产库支持按门店、资产来源、产品精修、品牌创作、短剧图片、创建人、状态和日期筛选。
8. 平台资产库的集中查看不改变业务隔离；跨模块再次引用仍需执行素材类型和权限校验。
9. 平台资产库保留上传功能。上传时必须先选择门店、归属业务和资产分类；未完成归属选择不能提交。
10. 平台资产库支持预览、批量选择、下载、重命名、移动至同业务子文件夹、归档和删除至回收站。
11. 普通用户不能通过平台资产库绕过模块隔离；例如 `retouch` 素材不会直接出现在品牌选择器中。

### 9.4 Git 项目内分级目录

Demo 阶段可把临时文件落在项目的 `storage/generated/` 下。建议目录结构：

```text
storage/
└── generated/
    └── {tenant_id}/
        └── {store_id}/
            └── {workspace}/
                └── ai-temp/
                    └── {YYYY}/{MM}/{DD}/
                        └── {task_id}/
                            ├── inputs/
                            ├── outputs/
                            ├── thumbnails/
                            └── manifest.json
```

示例：

```text
storage/generated/default/day-coffee-night-bar/retouch/ai-temp/2026/09/15/{task_id}/outputs/result-01.png
```

`manifest.json` 保存任务 ID、用户、门店、业务空间、模型、Skill、提示词摘要、输入/输出文件名、创建时间、到期时间和文件校验值，但不保存 API 密钥。

### 9.5 Git 与部署风险

项目当前已通过 `.gitignore` 忽略 `storage/`，应继续保持。这里所说的“保存到 Git 的分级文件夹”应理解为**保存到 Git 项目的工作目录**，而不是执行 `git add` 或提交图片。

禁止把生成图片提交进 Git，原因包括：

- 二进制图片会永久占用 Git 历史，删除工作区文件也无法缩小历史。
- 高频生成会使克隆、拉取、部署和备份越来越慢。
- 用户素材可能包含商业或个人信息，不适合进入代码版本历史。
- 部分线上平台文件系统是只读或重启即清空，不能把项目目录当永久云盘。

Demo 本地运行可使用上述目录；线上部署时必须使用带持久磁盘的服务器，或把同样的逻辑目录映射到对象存储。数据库只保存逻辑键和元数据，不依赖某台机器的绝对路径。

### 9.6 临时保留与清理

- 默认保留期建议为 7 天，由管理员配置为 1—30 天。
- 文件卡片显示具体到期日期，不只显示“即将过期”。
- 到期前 24 小时显示提醒；用户可立即下载或在权限允许时延长一次。
- 定时清理任务先把记录标记为 `expiring`，确认无进行中的编辑任务后删除原图与缩略图。
- 清理失败进入重试队列并记录日志，不能只删数据库记录而留下孤儿文件。
- 用户主动删除先进入 24 小时回收区，之后物理删除。
- 已下载不等于永久保留，仍按到期时间清理。

### 9.7 下载体验

- 单张图片支持“下载原图”，文件名包含门店、业务、日期和短任务号。
- 多选后支持 ZIP 打包下载，并生成下载任务进度。
- 下载使用登录校验后的短时签名 URL，禁止暴露服务器绝对路径。
- 下载前校验文件仍存在；已过期时显示重新生成或联系管理员，不返回空文件。
- 下载不改变图片所在素材空间，也不会自动复制到其他素材库。

## 10. UI、反馈与性能体验规范

### 10.1 操作原则

- 每个页面只突出一个当前主操作。
- 高频路径最多三步到达执行确认。
- 不因页面切换丢失上传队列、编辑内容或筛选条件。
- 所有生成结果都明确说明保存位置和可执行的下一步。
- 高风险操作使用确认框；普通保存使用轻提示，不频繁弹窗。

#### 工作台入口可点击规则

图二中以下区域必须是明确的可点击入口，不能仅作为展示装饰：

| 工作台区域 | 点击结果 |
| --- | --- |
| 顶部“产品精修”模块标签 | 进入产品精修 / 一键修图 |
| 顶部“品牌创作”模块标签 | 进入品牌创作 / 素材创作 |
| 顶部“短剧脚本”模块标签 | 进入短剧创作 / 资产库 |
| “5 张精修小样”待确认项 | 进入对应精修任务详情并定位待确认步骤 |
| “脚本第一集”待审项 | 打开对应剧本版本的审阅页面 |
| 每日爆款视频内容 | 进入对应分析/任务详情；若功能未接入则显示明确的未开放说明，不做无反馈点击 |
| “继续创作”项目卡片 | 恢复该项目最后一次编辑状态 |
| 卡片右侧箭头 | 与整张卡片保持相同跳转，不设置不同的隐藏行为 |
| “查看全部” | 进入当前用户有权限查看的历史项目列表 |

整张卡片均可点击，鼠标悬停显示指针和轻微抬升；键盘可使用 Tab 聚焦并按 Enter/Space 打开。跳转后必须有返回路径，浏览器前进/后退可正常工作，不能只绑定无 URL 的临时组件状态。

### 10.2 快速反馈

- 点击、选择和上传在 100 毫秒内出现视觉反馈。
- 上传显示逐文件进度和失败原因。
- AI 任务在 1 秒内返回“已接收”，随后展示真实阶段，不使用虚假百分比。
- 长任务允许用户离开页面，并通过全局任务中心查看进度。
- 保存状态固定在编辑器可见位置。
- 错误文案包含原因、影响和下一步，如“第 7 张图片格式不支持，可移除后继续执行其余 12 张”。
- 支持取消尚未执行的任务、重试失败项和恢复中断任务。

#### 点击反馈规范

- 所有可点击元素必须具备默认、悬停、按下、焦点、禁用和加载状态。
- 按下反馈在下一帧出现，视觉目标不超过 50ms；不得等待 API 返回后才改变按钮。
- 提交类按钮点击后立即进入加载状态并防止重复提交，同时提供取消或返回能力。
- 页签切换先立即更新选中态，再加载内容；内容区使用骨架屏，不能整页白屏。
- 卡片选择、展开、收藏和筛选采用前端即时更新；请求失败时回滚并解释。
- 成功提示保持 2—3 秒且不遮挡后续操作；保存类成功优先使用行内状态。

### 10.3 流畅度与性能预算

以下指标以常见办公网络和中端笔记本为验收环境：

| 指标 | 目标 |
| --- | --- |
| 首次可操作时间 | 缓存后 ≤ 1.5 秒；首次访问 ≤ 2.5 秒 |
| 页签/路由切换反馈 | 选中态 ≤ 50ms，主要内容可见 ≤ 300ms |
| 点击到视觉反馈 | ≤ 100ms，目标值 50ms |
| 搜索/筛选响应 | 本地数据 ≤ 100ms；服务端结果首屏 ≤ 500ms |
| 输入框响应 | 连续输入无可感知卡顿，主线程单任务 ≤ 50ms |
| 滚动性能 | 目标 60fps，低配设备不低于 45fps |
| 图片首屏 | 先展示缩略图/模糊占位，不等待原图 |
| AI 请求受理反馈 | ≤ 1 秒返回任务已创建状态 |

实现要求：

- 页面按路由和功能拆包；不在首屏加载视频 SDK、编辑器或全部图库原图。
- 图片生成多档缩略图，使用 WebP/AVIF、懒加载、尺寸占位和 CDN 缓存。
- 图库超过 100 项时使用分页或虚拟列表，禁止一次渲染全部高清图片。
- 鼠标悬停只预取可能进入的详情数据，不预取大图和视频。
- 输入搜索使用 150—250ms 防抖；选中、计数等本地操作不得走网络往返。
- 保留页面组件和请求缓存，来回切换页签不重复下载相同数据。
- 请求支持取消和去重；快速切换筛选时丢弃旧响应，避免列表闪回。
- AI 生成通过后台任务执行，页面不阻塞；任务状态采用推送或合理轮询。
- 减少大面积 `backdrop-filter`、持续模糊和多层阴影；弱性能设备自动降级玻璃效果。
- 动画只改变 `transform` 与 `opacity`，避免引发布局抖动；列表卡片不做持续动画。
- 对按钮重复点击、慢接口、断网和请求超时设置统一保护与恢复策略。

### 10.4 加载状态设计

- 首屏：显示与真实布局一致的骨架，不使用全屏旋转图标。
- 图片：先展示固定比例占位，再渐进加载缩略图；原图只在预览时加载。
- 列表刷新：保留旧内容并显示局部刷新状态，避免内容突然清空。
- 上传：显示文件级进度、剩余数量、暂停/取消和失败项。
- AI 任务：显示当前阶段和已耗时，不展示无法保证准确的完成百分比。
- 空结果：区分“没有数据”“筛选无结果”“加载失败”，对应提供不同动作。

### 10.5 视觉舒适度

- 延续咖啡、酒与茶馆的暖色文化气质，以米白、深咖、茶绿为主，潮流色只用于状态和重点操作。
- 大面积内容区保持低对比纯净背景，减少玻璃层叠、持续闪烁和大幅动态渐变。
- 动效只用于状态切换、页面进入和任务完成，单次 150—240 毫秒。
- 遵循系统“减少动态效果”设置。
- 同屏主按钮不超过两个；次要操作收纳进菜单。
- 卡片密度可切换舒适/紧凑，以适应设计师批量处理。

#### 字号与可读性基线

本版统一放大截图中偏小的助手、输入和页签文字：

| 内容层级 | 桌面端建议字号 | 最低要求 |
| --- | --- | --- |
| 页面主标题 | 30—34px | 28px |
| 对话框/面板标题 | 18—20px | 18px |
| 正文、输入内容、生成计划 | 16—18px | 16px |
| 按钮、页签、字段值 | 15—16px | 15px |
| 字段标签、卡片说明 | 14—15px | 14px |
| 辅助状态与时间 | 13—14px | 13px，仅限非关键辅助信息 |

- 正文行高 1.6—1.75，输入框行高不低于 1.6。
- 字体颜色与背景对比度满足 WCAG AA；不能用低透明度浅灰显示关键说明。
- 表单字段高度不低于 44px，主要按钮高度不低于 44px。
- 不以缩小字号解决内容溢出，应优先换行、扩展面板或提供内部滚动。
- 1440px 以上屏幕不继续缩小文字；浏览器缩放到 200% 时功能仍可操作。

### 10.6 全平台图片检视与缩放

所有真实生成完成的图片，无论出现在产品精修结果、品牌产品库、板块业务库、任务详情还是平台资产库，都必须可以点击打开统一图片查看器。

#### 打开与关闭

- 单击缩略图打开大图查看器；卡片上的复选框和更多菜单不触发打开。
- 查看器使用覆盖层或独立详情页，背景弱化但不丢失原列表位置。
- 支持 Esc、关闭按钮和点击安全遮罩关闭；存在未保存标注时需确认。
- 关闭后回到原卡片并恢复滚动位置、筛选和选择状态。

#### 缩放与移动

- 鼠标滚轮以指针所在位置为中心放大/缩小。
- 支持工具栏 `－`、`＋`、适应窗口、100% 原始尺寸和全屏。
- 支持触控板双指缩放、移动端双指缩放。
- 图片放大后可按住鼠标拖拽查看，光标显示抓取状态。
- 建议缩放范围为 10%—800%，每次缩放步长平滑且显示当前百分比。
- 双击在“适应窗口”和“100%”之间切换。
- 图片不能被拖出完全不可见区域；提供一键复位。

#### 检视信息与操作

- 展示文件名、像素尺寸、格式、文件大小、生成时间、模型和任务 ID。
- 产品精修支持原图/结果并排、滑杆对比和版本切换。
- 多张结果支持左右切换、键盘方向键和缩略图胶片栏。
- 查看器内可下载原图、继续编辑、复制任务参数；删除仍需二次确认。
- 首先加载适配屏幕的预览图，用户选择 100% 或继续放大时再加载原图，避免打开时卡顿。
- 原图加载失败时保留预览并显示重试，不直接关闭查看器。

### 10.7 空状态与新手引导

每个空页面都需提供一个明确动作：

- 产品图库为空：上传第一张产品图。
- 品牌产品库为空：开始第一次素材创作。
- 短剧资产库为空：创建茶馆场景卡。
- 剧本库为空：选择场景并让编导助手创建剧本。
- 视频生成无可用剧本：前往剧本库保存或通过一个版本。

## 11. 核心数据对象

### Asset

`id`、`tenant_id`、`store_id`、`asset_space`、`folder_type`、`category`、`name`、`storage_key`、`thumbnail_key`、`mime_type`、`size`、`checksum`、`is_temporary`、`expires_at`、`metadata`、`source_asset_id`、`created_by`、`created_at`。

### AgentTask

`id`、`tenant_id`、`store_id`、`workspace`、`skill_id`、`skill_version`、`task_type`、`input_assets`、`requirements`、`plan`、`final_prompt`、`provider`、`model`、`provider_request_id`、`status`、`progress_percent`、`progress_stage`、`heartbeat_at`、`started_at`、`completed_at`、`error_code`、`output_ids`、`cost_usage`、`created_by`、`created_at`。

### TaskEvent

保存任务状态时间线：`id`、`task_id`、`event_type`、`from_status`、`to_status`、`progress_percent`、`stage`、`message`、`provider_payload_ref`、`created_at`。用于判断卡住、刷新后恢复进度和审计模型调用。

### MasterAssetIndex

`id`、`asset_id`、`tenant_id`、`store_id`、`asset_space`、`source_task_id`、`sync_status`、`visibility`、`expires_at`、`deleted_at`、`created_at`。只建立索引，不保存第二份原图。

### CreativeWork

统一记录精修成品与品牌成品：`id`、`workspace`、`title`、`type`、`status`、`versions`、`source_assets`、`task_id`、`approved_by`、`updated_at`。

### Script

`id`、`title`、`status`、`current_version_id`、`scene_asset_ids`、`characters`、`synopsis`、`episodes`、`created_by`、`updated_at`。

### ScriptVersion

`id`、`script_id`、`version_no`、`content`、`change_note`、`model_trace`、`saved_by`、`saved_at`。

### VideoTask

`id`、`script_id`、`script_version_id`、`scene_ids`、`shot_plan`、`provider`、`model`、`settings`、`status`、`outputs`、`cost_estimate`、`created_at`。

## 12. v0.7 Demo 范围

### 必须实现

- 新的板块内页签与左侧导航结构。
- 产品精修的三种模式选择和专属图库。
- 批量上传队列、统一参数、逐项状态与失败项重试。
- 品牌素材创作到产品库的自动归档。
- 三个模块的素材查询和引用严格隔离。
- 短剧资产库、服务端剧本库、可编辑正文、自动保存和版本保存。
- 从已保存剧本进入视频生成并完成前置校验。
- 三个板块的独立智能体上下文。
- 真实任务状态、错误提示、重试和断点恢复入口。
- 账号密码登录与角色权限基础能力。
- 产品库、剧本库和图库的纯库页面布局，不常驻完整机器人对话。
- 品牌案例和剧本案例按本版规则迁入对应库。
- 批量选图复选框、选中编号、计数与固定操作栏。
- 图片懒加载、路由拆包、请求缓存与性能监控基础能力。
- 至少接通一个真实图片编辑模型和一个真实图片生成模型；如果同一模型同时支持两种能力，可复用同一供应商适配器。
- 修图助手和生图机器人均通过对应 Skill 生成可追溯的最终提示词。
- 模型返回的真实图片自动保存到当前门店、当前业务的“AI 临时生成”子文件夹。
- 临时图片的缩略图、到期时间、下载、批量 ZIP 下载和主动删除能力。
- `storage/generated/` 保持 Git 忽略状态，并具备定时清理任务。
- 三个功能板块提交任务后立即重置创作区，允许连续创建任务。
- 服务端任务队列、状态持久化、心跳、进度事件和刷新恢复。
- 各业务库展示运行中、失败、完成和取消任务及阶段化百分比。
- 历史记录筛选、详情、复用、取消、重试和删除至回收站。
- 素材引用小窗的鼠标滚轮横向浏览、滚动位置恢复和大数据量优化。
- 保留左侧一级“资产库”，完成统一索引、分类上传、权限过滤和同步失败重试。
- 生成图片自动同步平台资产库索引，但只保存一份物理文件。
- 将 G-01—G-08 全部纳入版本范围；P0 未通过前不得宣称 v0.7 完整交付。
- 产品精修选择素材后自动识别并回填动态词条，支持置信度和识别来源展示。
- 用户编辑保护、字段锁定、单字段恢复 AI 建议和重新识别。
- 同尺寸的需求识别框与提示词确认框，以及可编辑的最终提示词。
- 工作台顶部模块入口、待办项、继续创作卡片和箭头的完整路由跳转。
- 品牌创作与短剧创作对话框按本版尺寸规范调整。
- 全局字号和表单尺寸达到 10.5 节的可读性基线。
- 所有生成图片支持点击打开、滚轮/按钮/触控缩放、拖拽、适应窗口、100% 和全屏检视。

### 可模拟但必须明确标注

- 尚未签约或接入的视频模型生成。
- 尚未接入的全网爆款短剧数据源。
- 尚未录入的完整 VI 规则自动校验。

### 本版不做

- 面向公众开放注册。
- 复杂组织、多租户计费和客户自助开通。
- 未经授权的平台爬取或内容搬运。
- 训练自有基础模型。
- 专业剪辑软件级时间线、配音和口型编辑。

## 13. 验收标准

### 产品精修

- 顶部只显示“一键修图、图库”两个页签。
- 一键修图内可选择上传图片、批量修图、修图模版。
- 批量任务单图失败时其余图片继续执行。
- 完成结果自动进入精修图库，且可追溯原图和任务。
- 产品精修选择器无法看到品牌或短剧素材。
- 图库中不出现添加图片、批量修图、修图模版或修图助手。
- 批量选择时每张选中图片都有明确勾选标记，页脚计数与实际选中数一致。
- 切换咖啡、酒、茶饮等不同素材时，预设词条随图片重新识别，不沿用无关固定文案。
- 用户人工修改或锁定字段后，后台重新识别不会覆盖该字段。
- 点击生成精修计划后进入同尺寸第二阶段面板，页面不跳动且输入不丢失。
- 用户可以编辑最终提示词；只有点击“确认并生成图片”才创建真实图片任务。

### 品牌创作

- 顶部只显示“素材创作、产品库”。
- 用户可选择或上传品牌素材并调用生图机器人。
- 生成结果无需二次上传，自动出现在产品库。
- 品牌素材选择器无法看到精修和短剧素材。
- 每个成品可查看提示词、引用素材和历史版本。
- 截图所示品牌案例只出现在产品库，不再出现在素材创作页历史列表中。
- 产品库默认不显示完整生图机器人，点击继续创作后才返回创作流程。
- 品牌需求输入、创意方向和结果区尺寸合理，不出现用于撑满屏幕的大面积无效空白。
- 品牌创作正文、按钮、页签和辅助说明达到字号最低要求。

### 短剧创作

- 顶部显示“资产库、剧本库、视频生成”。
- 用户能从场景资产发起剧本创作。
- 生成剧本自动进入剧本库，可编辑、自动保存、手动存版本和恢复。
- 视频生成只能选择已保存的剧本版本。
- 缺少必要场景或分镜信息时，系统能阻止执行并指出缺失项。
- 短剧资产选择器无法看到精修和品牌素材。
- 截图所示剧本案例只出现在剧本库，不再出现在资产库。
- 剧本库首页不常驻大段机器人对话，打开编辑器后才加载 AI 编辑助手。
- 剧本需求输入框和大纲编辑区达到本版规定高度，长内容可读且可滚动。
- “生成脚本大纲”按钮宽度合理，不在桌面端横跨整个页面。

### 通用体验

- 用户进入任一板块后，能在三步内到达任务确认。
- 所有异步操作均有即时反馈和明确状态。
- 刷新或跨页后，已保存任务、作品和剧本仍可恢复。
- 失败信息可操作，不只显示“生成失败”。
- 三个角色看到的权限与操作范围符合定义。
- 360px、768px、1440px 视口下核心流程可用，无横向内容溢出。
- 点击、页签切换、筛选和首屏性能达到 10.3 节定义的预算。
- 图片未加载完成时页面尺寸稳定，不发生明显跳动。
- 弱网络下可看到真实加载阶段，用户不会因无反馈重复点击。
- 工作台截图所指模块标签、待办项、继续创作卡片和箭头均能进入正确页面。
- 所有生成结果缩略图均可打开统一查看器，并支持 10%—800% 缩放和拖拽检视。
- 图片查看器关闭后恢复原页面滚动、筛选和选择状态。
- 关键正文与输入内容不小于 16px，字段标签不小于 14px。

### 后台任务与历史记录

- 三个功能中任一任务提交成功后，300ms 内出现新的空白创作界面。
- 服务端未返回任务 ID 时页面不清空，所有输入和素材选择保持不变。
- 上一任务在业务库与任务中心 1 秒内可见，状态与任务 ID 一致。
- 页面刷新、退出重登和服务重启后，任务记录、进度与结果不丢失。
- 进度只能前进不能倒退；失败、取消除外，完成必须准确显示 100%。
- 模型无细粒度进度时显示阶段进度，不出现自动匀速增长的假百分比。
- 超过心跳阈值的任务能显示“疑似卡住”，超时后能重试并释放并发位。
- 运行中任务无法直接删除；历史删除遵循回收站和引用保护规则。
- 素材小窗可用普通鼠标滚轮完整浏览，滚到边界后页面可以继续滚动。
- 关闭并重新打开素材窗后，已选素材、筛选和滚动位置均可恢复。

### 平台资产库

- 左侧一级“资产库”入口必须保留且可正常进入。
- 每张生成完成且校验通过的图片同时出现在所属业务库和平台资产库。
- 两个入口指向同一个 `asset_id` 和同一份物理文件，不产生重复图片。
- 平台资产库同步失败时业务库图片仍可正常查看和下载，并明确提示同步状态。
- 同步任务可自动重试，成功后无需刷新整页即可出现在平台资产库。
- 删除、到期和权限变更能同步反映到平台资产库，不能残留失效入口。
- 从平台资产库上传时必须选择门店、业务和分类，素材选择器只能读取与当前功能相符的资产。
- 平台资产库支持搜索、筛选、预览、多选、下载、归档和删除至回收站。

### 模型、Skill 与临时存储

- 输入自然语言后，可查看由正确 Skill 生成的结构化计划与最终提示词摘要。
- 用户确认后系统真实调用模型 API，并显示本次使用的模型与任务状态。
- 成功结果必须是本次模型返回的可打开图片，而不是案例图或占位图。
- 精修咖啡产品时，结果默认出现在“日咖夜酒 / 产品精修 / AI 临时生成”。
- 切换到茶馆后生成的图片只能出现在茶馆对应的临时文件夹。
- 每个图片文件、数据库记录和 `manifest.json` 的任务 ID、门店和业务空间一致。
- 用户可以下载单张原图；选择多张后可下载有效 ZIP。
- 未配置模型、模型超时、远程 URL 过期和本地写入失败均有独立错误提示与重试路径。
- 到期清理后文件不可访问，数据库保留最小审计记录并标记已清理。
- 执行 `git status` 时，生成图片不出现在待提交文件列表中。

## 14. 建议实施顺序

1. 定义门店、资产空间、临时文件和任务数据结构，完成服务端权限过滤。
2. 创建 `storage/generated/` 分级目录服务、缩略图、清单和 Git 忽略校验。
3. 建立统一模型网关与供应商适配器，先验证真实图片生成和下载。
4. 固化两个图片 Skill 的输入输出契约，并保存最终提示词与版本。
5. 打通产品精修“输入→计划→确认→真实修图→咖啡店临时图库→下载”。
6. 打通品牌创作“品牌素材→提示词→真实生图→品牌临时产品库→下载”。
7. 加入批量任务、失败项重试、ZIP 下载和临时文件定时清理。
8. 建立统一后台任务队列、任务事件、心跳、进度推送和历史恢复。
9. 重构三个创作页为提交即重置，并在对应业务库显示实时任务卡。
10. 完成素材小窗滚轮浏览、选择状态保持、搜索和虚拟列表。
11. 完成平台资产库的统一索引、分类上传、权限过滤和可靠同步/补偿任务，验证不重复文件。
12. 再完成导航、纯库页面、剧本持久化和视频任务的其余 v0.5 要求。
13. 在线部署前把本地存储接口切换或映射至持久磁盘/对象存储，并进行重启恢复测试。
14. 完成权限、费用、并发、文件过期、响应式和真实素材小规模验收。

## 15. v0.7 成功指标

- 设计师能在一个会话内完成 50 张产品图的上传、统一设置和批量提交。
- 品牌图片生成后 100% 自动归档，不需要手动下载再上传。
- 编导能从真实茶馆资产发起脚本，完成编辑并保存为可用于视频生成的版本。
- 跨模块素材误选率为 0。
- 页面关键操作均有可感知反馈，失败任务可定位并重试。
- 老板能查看三类工作的近期产出、任务状态和存储用量。
- 用户在批量选图测试中能一眼识别全部已选素材，选中数误判率为 0。
- 库页面与创作页面职责测试正确率达到 100%，用户不会在图库寻找机器人或在创作页寻找历史成品。
- 图片任务真实生成成功率达到已接模型正常请求的 95% 以上，供应商失败不计入伪成功。
- 真实生成图片自动归档正确率为 100%，门店或业务错放为 0。
- 用户无需经过服务器文件目录即可完成预览和下载。
- 临时文件到期清理成功率达到 99%，不存在可持续累积的孤儿文件。
- 任务提交后新建下一任务的等待时间不超过 300ms。
- 历史任务刷新恢复率为 100%，任务与成品不会因浏览器刷新消失。
- 卡住任务能在配置阈值内被识别，任务状态无法永久停留在无解释的“生成中”。
- 完成图片进入平台资产库的索引同步成功率达到 99.9%，失败项可自动补偿。
- 不同产品素材的动态词条识别与人工确认匹配率达到 90% 以上。
- 用户修改字段被 AI 意外覆盖的次数为 0。
- 工作台标注入口的有效点击跳转率为 100%。
- 用户可在不下载图片的情况下完成细节放大检视和原图/结果对比。

---

本文件为 v0.7 Demo 的产品与交互基线。实现中若与 v0.1—v0.6 冲突，以本文件为准；旧版本文档保留作为版本历史。
