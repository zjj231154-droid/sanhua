# 短剧制作工作台

本项目安装了项目级 Skill：`.agents/skills/short-drama-production/`。

处理短剧立项、剧本、角色、分集、合规、资产、分镜、图片/视频提示词、连续性、生成迭代或交付任务时，必须先读取该 Skill 的 `SKILL.md`，再按阶段加载最少量参考资料。

默认在 `projects/` 下创建实际短剧项目。未经用户明确授权，不调用付费生成模型，不上传、不发布、不部署。
