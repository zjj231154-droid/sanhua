[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --ignore-scripts --cache /tmp/npm-cache --prefer-offline=false"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
